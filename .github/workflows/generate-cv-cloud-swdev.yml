name: "Generate CV: Cloud Software Developer"

on:
  workflow_dispatch:

permissions:
  contents: write
  models: read

jobs:
  generate-swdev-cv:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # ── Tailor README for Cloud Software Developer using GitHub Models API ──
      - name: Tailor README for Cloud Software Developer
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x ./src/tailor_readme.sh
          ./src/tailor_readme.sh cloud-swdev

      # ── GitHub Stats SVGs (needed for PDF rendering) ──
      - name: Create artifacts directory
        run: mkdir -p artifacts/profile

      # ── Generate PDF from tailored README ──
      - name: Generate PDF from tailored README
        run: |
          chmod +x ./src/generate_role_pdf.sh
          ./src/generate_role_pdf.sh cloud-swdev

      # ── Commit role-specific artifacts ──
      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add artifacts/cloud-swdev/
          git diff --cached --quiet || (git commit -m "docs: generate Cloud Software Developer tailored CV [skip ci]" && git push)
