name: "Generate CV: Cloud SRE"

on:
  workflow_dispatch:

permissions:
  contents: write
  models: read

jobs:
  generate-sre-cv:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # ── Tailor README for Cloud SRE using GitHub Models API ──
      - name: Tailor README for Cloud SRE
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x ./src/tailor_readme.sh
          ./src/tailor_readme.sh cloud-sre

      # ── GitHub Stats SVGs (needed for PDF rendering) ──
      - name: Create artifacts directory
        run: mkdir -p artifacts/profile

      # ── Generate PDF from tailored README ──
      - name: Generate PDF from tailored README
        run: |
          chmod +x ./src/generate_role_pdf.sh
          ./src/generate_role_pdf.sh cloud-sre

      # ── Commit role-specific artifacts ──
      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add artifacts/cloud-sre/
          git diff --cached --quiet || (git commit -m "docs: generate Cloud SRE tailored CV [skip ci]" && git push)
